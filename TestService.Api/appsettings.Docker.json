{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "MongoDbSettings": {
    "_comment": "Dev fallback. Compose overrides MongoDbSettings__ConnectionString from MONGO_PASSWORD env var (defaults to password123 to keep existing volumes working).",
    "ConnectionString": "mongodb://admin:password123@mongodb:27017/TestServiceDb?authSource=admin",
    "DatabaseName": "TestServiceDb",
    "CollectionName": "TestData"
  },
  "RabbitMqSettings": {
    "HostName": "rabbitmq",
    "Port": 5672,
    "UserName": "guest",
    "Password": "guest",
    "ExchangeName": "test-service-exchange",
    "QueueName": "test-data-queue"
  },
  "JwtSettings": {
    "_comment": "Dev-only placeholder. Compose overrides JwtSettings__SecretKey from JWT_SECRET_KEY env var. Production must set JWT_SECRET_KEY.",
    "SecretKey": "dev-only-do-not-use-in-prod-32chars-padding-required-here",
    "Issuer": "TestServiceApi",
    "Audience": "TestServiceClients",
    "ExpirationMinutes": 60
  }
}
